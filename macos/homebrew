function homebrew_clean_up() {
    # remove outdated versions from cellar
    brew cleanup > /dev/null
}

function __homebrew_on_exit() {
    unset -f homebrew_clean_up
    unset -f __homebrew_on_exit
}
trap __homebrew_on_exit EXIT
